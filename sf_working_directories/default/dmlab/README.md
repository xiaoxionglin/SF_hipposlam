## Installation

Main dmlab package needs to be built from sources (see dmlab github repo) or better be provided by Xiao or Jannek. Also add the custom environments (dmlab_env.py references those).

Besides:

```shell
sudo apt install libosmesa6-dev
pip install dm_env
```
